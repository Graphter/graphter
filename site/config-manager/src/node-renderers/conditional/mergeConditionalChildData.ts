import { MergeChildDataFn } from "@graphter/core";

export const mergeConditionalChildData: MergeChildDataFn = (config, path, internalNodeData, getExternalPathData, childData) => {
  return childData
}