import { MergeChildDataFn } from "@graphter/core";

export const mergeConditionalChildData: MergeChildDataFn = async (config, path, internalNodeData, getExternalPathData, childData) => {
  return childData
}