import ManageConfig from "./components/ManageConfig";
import React, { useState } from "react";

export default function App(){
  return (
    <main className='container p-10 mt-10 rounded-2xl bg-white'>
          <ManageConfig />
    </main>
  )
}